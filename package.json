{
  "name": "aegis-ios",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "format": "prettier --write package.json tsconfig.json eslint.config.mjs .prettierrc.json redocly.yaml docker-compose.yml src tests openapi db",
    "format:check": "prettier --check package.json tsconfig.json eslint.config.mjs .prettierrc.json redocly.yaml docker-compose.yml src tests openapi db",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "contract:check": "NO_UPDATE_NOTIFIER=1 redocly lint openapi/openapi.yaml",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm contract:check"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@redocly/cli": "2.46.0",
    "eslint": "10.8.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.66.0",
    "vitest": "4.1.10"
  }
}
